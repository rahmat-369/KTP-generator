const { createCanvas, loadImage, GlobalFonts } = require('@napi-rs/canvas');
const axios = require('axios');

// ... (kode ktp.js Anda tetap sama persis dari file awal)
// Copy semua kode dari ktp.js awal ke sini